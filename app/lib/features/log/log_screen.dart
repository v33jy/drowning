import 'package:flutter/material.dart';

/// Placeholder — merged 탐지 이력 + 알림 센터 screen (SegmentedButton: 전체/미확인).
/// Real content is built across the History/Notification steps.
class LogScreen extends StatelessWidget {
  const LogScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('기록 화면 — History/Notification 단계에서 구현')),
    );
  }
}
