opencv-python-headless>=5.0.0.93,<6
numpy>=1.24,<3
websockets>=17.0

# picamera2 is not listed here — it's preinstalled on Raspberry Pi OS and
# isn't pip-installable on a regular dev machine. Only needed for the real
# camera path; --mock runs fine without it.
